# STATE

## Goal
Build multifactor equity trading system (Grinold & Kahn framework) — learn quant-shop internals + resume piece. Currently design-capture phase.

## Now
Two-phase backfill (2026-07-14): overnight phase 1 died silently (caffeinate -t 300 expired, Mac slept, no year part written). Relaunched under untimed `caffeinate -i` → COMPLETE: 1,459,612 rows / 250 days / 6,139 of 7,092 tickers landed in 1,161 s (1,257 rows/s), audit ok, recorded in docs/METRICS.md. 246 tickers hit YFRateLimitError (all micro-cap/SPAC-tier — top-1500 selection effectively unbiased). Select-top done: tickers_top1500.txt (top 5: TSLA NVDA AAPL MSFT PLTR). Biased 2011–2024 parts wiped (Ethan approved 2026-07-14; 2025 part kept). Phase 2 ran 12:02–12:56 EDT 2026-07-14 (`caffeinate -i .venv/bin/python -u -m research.data.loaders.yfinance_daily --start 2011 --no-threads --chunk-size 50 --tickers-file tickers_top1500.txt`): parts 2011–2025 landed under k=2026-07-14T16-02-12, every year audit ok (~3.5 min/year, ~370k rows/recent year). Task then KILLED (~12:57, not by Claude — cause unknown) mid-2026 fetch: 2026 YTD part MISSING. Gap filled 15:09 EDT (198,000 rows / 132 days / 0 missing, audit ok). BACKFILL COMPLETE: 2011–2026 YTD × top-1500 in lake (4.82M rows, 143 MB incl. phase 1 part), both phases + resume phrasing recorded in docs/METRICS.md. Universe builder (part 3) BUILT same day: research/universe.py (month_end_trading_days / build_snapshot / build_universe / CLI), 9 tests, full suite 35 passed; real run wrote lake/universe_monthly (185 snapshots 2011-03→2026-07, 183,464 rows, 235.7s) — recorded in METRICS.md; SYSTEM_MAP.md updated. Mcap filter deferred to CRSP (no shares data). Note: latest snapshot dated 2026-07-14 = partial-month rebuild (last trade date of current month).

Security master blocks 1-3 BUILT same day: research/security_master.py (DATASET, SCHEMA, empty_master(), _ticker_segments, build_ticker_segments, CLI, resolve_securities, resolve_security), tests/test_security_master.py (14 tests), full suite 45 passed; SYSTEM_MAP.md + METRICS.md (point-query benchmark + block 2/3 real-run numbers) + handoff.md updated. lake/security_master populated (6,141 rows). Only part 4 block 4 (CRSP hookup, WRDS-blocked) remains before security master is feature-complete for the yfinance era.

Phase 2 built 2026-07-16/17: research/signals/ (momentum.py, reversal.py, low_vol.py, __init__.py's SIGNAL_REGISTRY) + research/alpha/ic.py (compute_forward_returns, compute_ic, build_ic_series, ic_summary). 15 new tests (test_signals.py, test_ic.py), full suite 61 passed. Real run over full 2011-2026 lake: momentum n=169 mean_ic=0.019 t=1.45, reversal n=180 mean_ic=0.0007 t=0.07, low_vol n=174 mean_ic=-0.0017 t=-0.10 — recorded in METRICS.md. Found + fixed a real perf bug mid-build: compute_forward_returns scanned full 15y history per call (3.3s) instead of a bounded forward window (1.2s after fix); also removed 3x redundant forward-return recomputation across signals in build_ic_series. See Done below.

Block 3 (Risk Model) DONE 2026-07-19: sector loader (2026-07-17, see below) + research/risk/ (exposures.py, regression.py, factor_covariance.py, specific_variance.py, model.py) all built and real-lake verified. 17 new tests, full suite 79 passed. Real run: `build_risk_model(store, "2026-07-14", lookback_years=3)` -> 985 securities, 13 factors, 179.5s, shrinkage=0.302, Σ symmetric + explicitly confirmed finite — recorded in METRICS.md. Two real bugs found+fixed against the real lake: (1) null sector values (3 unresolved tickers) crashed `sorted()` in exposures.py — fixed by filtering `is_not_null()` before computing the reference-sector drop; (2) the loader's known first-bar-of-year null `ret` flowed unfiltered into regression.py's target vector, became NaN via `.to_numpy()`, corrupted lstsq — fixed by filtering there too. A third apparent issue (persistent RuntimeWarnings even after both fixes) was root-caused to numpy's Accelerate BLAS backend on this Mac (documented, confirmed benign via explicit isnan/isinf checks on every output value) — NOT a bug, see METRICS.md + model.py's RiskModel.sigma() docstring.

## Next
1. Part 4 split into 4 blocks (2026-07-14, explained to Ethan): 1=schema/store-fit (DONE), 2=ticker extraction via trading-gap heuristic (DONE — real run: 6,139 tickers, 6,141 segments, 2 gap-detected but both confirmed vendor data gaps not real reuse, see METRICS.md), 3=resolve_securities panel lookup + resolve_security scalar wrapper (DONE — real-lake probe: MKC resolves correctly on both sides of its vendor gap, None inside it), 4=CRSP permno/CUSIP hookup (blocked, see below).
2. Broker simulator skeleton (2026-07-16) DONE — see Done below. Remaining Block 5 pieces (order gateway state machine, position keeper, pre-trade risk checks, market data handler, live AlpacaGateway) are separate, not yet scoped.
3. Phase 2: signals + IC (2026-07-16/17) DONE — momentum/reversal/low_vol built, registry-based, real IC numbers recorded (see Now/Done). Ethan flagged more (non-"raw and simple") signals coming later — registry pattern in research/signals/__init__.py exists specifically so a 4th signal is one line, not a rewrite.
4. Block 3: Risk Model DONE 2026-07-19 (see Now/Done). Block 4 (Optimizer) BUILT 2026-07-20/21/22/23: module split (research/portfolio/{beta,inputs,constraints,solve,model}.py — path corrected to match DESIGN.md's locked repo-layout, which names this directory `portfolio/` not `optimizer/`). Block 4a DONE (inputs.py + beta.py + ADV wiring). Block 4b DONE (constraints.py — full DESIGN.md constraint set except the documented borrow-data gap). Block 4c DONE (solve.py — QP objective + cvxpy solve). Block 4d DONE (model.py — `build_target_portfolio` chains inputs→solve into one call, mirrors research/risk/model.py's `build_risk_model` role; no CLI, matching that same mirrored module's actual precedent — see Done below). Block 4 (Optimizer) now fully built. Next in DESIGN.md's phasing order: Backtester, not yet scoped.
5. BLOCKED until fall semester (~late Aug 2026): live CRSP pull — Duke WRDS seasonal access. Loader code done + tested, runs when access returns. Security master block 4 also waits on this.

## Open items
(volume dtype bug — see Done below, FIXED 2026-07-15)

## Constraints
- "You can write plans, but do not start coding yet" (2026-07-08)
- "I will tell you more stuff, do not code anything yet" (2026-07-08)
- "Everything should be written down and remembered" (2026-07-08)
- "do not code anything yet" (repeated with infra message, 2026-07-08)
- "I want you to tell me what you build specifically in small parts for each system before you do it. I want the nitty gritty details, detailing each block of code and what it does" (2026-07-09) — learning project: explain-before-build, per small part, every system
- "While building the system, please write in a Metrics.md file metrics for things like latency, storage size, speed up, throughput, and other metrics that will be featured on my resume" (2026-07-09) — docs/METRICS.md; measured numbers only, with command + date + hardware
- "I want you to build a diagram of all the systems in this project as you add them, with each file, and what the functions in the file do, with arrows and other signals so that I know what each file is for" (2026-07-13) — docs/SYSTEM_MAP.md; update in the same session as any file/function change

## Decisions
- DECISION: C++ hot path + Python research stack — real quant-shop split, strongest resume signal
- DECISION: Alpaca paper trading — live data + real order lifecycle, zero money risk
- DECISION: US equities, free data only — richest factor literature
- DECISION: Long-short dollar+sector neutral — pure alpha, what stat-arb pods run
- DECISION: Daily rebalance — max breadth on free data
- DECISION: Universe = top ~1,000 US equities by 60d median dollar volume, point-in-time membership
- DECISION: Infra = Databricks notebooks/Workflows + Spark + Delta Lake on S3; AWS EC2/ECR/S3 — Ethan's choice, industry-standard batch stack
- DECISION: Storage split = parquet+Delta data lake (research/batch) and kdb+ (live trading time-series) — cheap warehouse vs high-perf live engine
- DECISION: PIT mechanism = explicit bitemporal columns primary, Delta time travel as audit layer — Delta vacuum/retention defaults too short for 5–15y PIT (Claude suggestion, in DESIGN.md)
- DECISION: No per-name stop losses; book-level protection only (portfolio drawdown kill switch, intraday loss alerts, fat-finger bounds) — Ethan chose "what real shops have"; stops fight signals + cut realized IC (2026-07-09)
- DECISION: Beta-neutral constraint added (β·w = 0) alongside dollar/sector neutrality — dollar-neutral alone leaves market exposure; needed to measure real alpha (2026-07-09)
- DECISION: Analytics suite added (per-factor P&L, Sharpe, IC, drawdown, capacity + dashboard) — Ethan requested factor-level measurement (2026-07-09)
- DECISION: yfinance = interim bars vendor (dataset `yfinance_daily`, keyed by ticker, backfill 2011→today) while WRDS season-blocked; replaced wholesale by CRSP in fall; never merged with crsp_daily before security master (approved spec 2026-07-13)
- DECISION: shares outstanding stored as separate one-row-per-ticker dataset `yfinance_shares_current` (effective_date = fetch date), optional `--shares` CLI flag — embedding current snapshot in 15y of bars would forge history; mcap deferred to CRSP (CONFIRMED by Ethan 2026-07-13)
- DECISION: yfinance backfill rate-limit strategy = paced run with retry/backoff in YFinanceClient (inter-chunk sleep + exponential backoff on YFRateLimitError; overnight single run) — chosen by Ethan 2026-07-13 over curated universe / top-ups / requests-cache
- DECISION: after paced full run failed (Yahoo stealth-throttle: fast "Data doesn't exist" fails, not YFRateLimitError; threads=True bursts 200 req/chunk so chunk pacing useless) → two-phase need-driven backfill: (1) slow-drip 1 recent year, all tickers, threads=False; (2) rank by dollar volume, backfill 15y for top ~1,500 only — chosen by Ethan 2026-07-14
- DECISION: Engine event-journaled from day one (crash recovery + trade blotter); broker simulator for deterministic engine tests; shadow deployment for new models; stress/crowding monitors; financing costs + live corporate-action handling added (2026-07-09)
- DECISION: engine/ build system = CMake, test framework = Catch2 v3.9.1 (FetchContent-pinned), C++20 — chosen by Ethan 2026-07-16 over Bazel/GoogleTest (simpler solo bootstrap, standard for C++ trading-system OSS)
- DECISION: IBrokerGateway event API = poll_events() over a queue, not OS callbacks — matches real HFT/quant practice (busy-spin over a lock-free ring buffer, e.g. LMAX Disruptor/Aeron-style), avoids callback/mutex latency jitter; chosen by Ethan 2026-07-16 after this tradeoff was explained
- DECISION: Phase 2 signal scope = momentum + reversal + low-vol only, NOT value/quality/size — those three need fundamentals (book equity, earnings, market cap) not yet ingested (mcap explicitly deferred to CRSP); data-availability fact, not a preference (2026-07-16)
- DECISION: IC horizon = 1 month = 21 trading days, matching the existing universe_monthly cadence (no new daily-universe infra needed); momentum = 12-1 month (Jegadeesh-Titman, skip-month excludes reversal contamination); all three signals store RAW values (no sign-flip) and let IC reveal the sign — chosen by Ethan 2026-07-16
- DECISION: research/signals/ uses a plain dict registry (SIGNAL_REGISTRY in __init__.py), one file per signal, uniform `fn(bars, rebuild_date) -> [effective_date, security_id, signal_value]` contract — Ethan flagged more (non-raw/simple) signals coming later, so adding one is "write the file, add one registry line," not a rewrite (2026-07-16)
- DECISION: Block 3 risk model built full-scope (sectors + style factors), not a sectors-skipped minimal version — Ethan chose to build the sector/industry loader first rather than defer sectors like value/quality/size were deferred in Phase 2 (2026-07-17)
- DECISION: Ledoit-Wolf shrinkage + Newey-West adjustment via scikit-learn (new dependency), not hand-rolled NumPy — correctness risk of hand-rolling the 2004 paper's optimal-shrinkage-intensity formula judged higher than the cost of one new, extremely standard dependency (2026-07-17)
- DECISION: factor covariance EWMA half-life = 63 trading days (~1 quarter) — chosen by Ethan 2026-07-17, DESIGN.md didn't specify a number
- DECISION: sector/industry source = yfinance `.info` (Yahoo's own taxonomy, 11 sectors matching GICS's count but not literal GICS codes), snapshot-only dataset `yfinance_sector_current` (same current-only limitation as `yfinance_shares_current`) — no free historical sector-reassignment source exists (2026-07-17)
- DECISION: Block 4 (Optimizer) TC(w-w_prev) proxy = simple quadratic turnover penalty κ·Σ(w_i - w_prev_i)², not linear or turnover-cap-only — standard placeholder until Block 5's real cost model exists, keeps the QP convex/quadratic without reformulation (chosen by Ethan 2026-07-20)
- DECISION: Block 4 solver = cvxpy (over hand-built OSQP matrices) — modeling-layer ergonomics for iterating on constraints outweighs the resume value of hand-deriving QP matrices; solve is once-daily, latency non-critical (chosen by Ethan 2026-07-20)
- DECISION: Block 4a alpha input placeholder = equal-weight blend of all 3 signals (z(momentum)+z(reversal)+z(low_vol))/3, NOT momentum-only — real alpha-combination weights are deferred to the alpha research phase (STATE Open items), this is a throwaway placeholder to exercise the full optimizer pipeline even though reversal/low_vol currently carry no real IC (chosen by Ethan 2026-07-20)
- DECISION: Block 4a missing-signal handling = drop the security that day if any of the 3 signals is absent (never average over a partial set) — consistent with the codebase's "absent means insufficient data, never fabricated" convention (chosen by Ethan 2026-07-20)
- DECISION: Block 4a alpha shrinkage = alpha_final = clip(0.5 * alpha, -3, 3) — fixed shrink factor 0.5 + hard cap ±3, both placeholder constants (no backtester yet to calibrate against), satisfies DESIGN.md's explicit "optimizer is an error maximizer" defense requirement (chosen by Ethan 2026-07-20)
- DECISION: Block 4b needs real per-stock beta for the β·w=0 beta-neutral constraint — discovered the risk model's "market" exposure column is a Barra-style constant (=1.0 for every stock, exposures.py), NOT a per-stock CAPM beta, so β·w=0 with it would just collapse to dollar-neutral. Fix: add research/portfolio/beta.py, rolling-OLS beta = cov(stock ret, universe ret)/var(universe ret) over trailing 252d, equal-weighted universe proxy (no mcap data yet for cap-weighting) — feeds a new `beta` vector into OptimizerInputs (chosen by Ethan 2026-07-21)
- DECISION: Block 4b borrow-availability filter DEFERRED, not built — no free borrow-rate/availability data source exists anywhere in the lake; DESIGN.md's own Block 5 section already flags this as hard-to-obtain free data. Documented gap, not a real choice to make yet (2026-07-21)
- DECISION: Block 4b ADV-relative position caps wired with real data — extends OptimizerInputs with a new `adv` vector (median_dollar_volume per security, joined from lake/universe_monthly, the same field the universe builder already computes) rather than a flat % placeholder — matches DESIGN.md's explicit "not just a fixed % of book" reasoning (chosen by Ethan 2026-07-21)
- DECISION: Block 4b book notional (dollars managed, needed to convert dollar ADV into a weight-space bound) = $10M placeholder — pure QP-scaling constant, doesn't touch real capital (Alpaca paper trading); realistic scale for a ~1,000-name long-short book so ADV caps bind on genuinely illiquid names rather than being a near no-op (chosen by Ethan 2026-07-21)
- DECISION: Block 4c risk aversion λ = 5.0 (weight on wᵀΣw in the objective) — placeholder, no principled value exists until a backtester can calibrate against realized Sharpe (chosen by Ethan 2026-07-22)
- DECISION: Block 4c soft turnover-cost weight κ = 10.0 (quadratic penalty on w-w_prev in the objective, ON TOP OF 4b's hard turnover_cap constraint — both intentional together: hard cap prevents blowups, soft cost shapes behavior inside it) — placeholder (chosen by Ethan 2026-07-22)
- DECISION: Block 4d non-optimal solve status (infeasible/unbounded) is surfaced, not raised — `build_target_portfolio` always returns a `TargetPortfolio` with `status` set to cvxpy's `problem.status`; caller must check it before trusting `weights`. Infeasibility (e.g. w_prev no longer satisfies that day's ADV/factor caps) is real backtester signal, not a bug to crash on — consistent with the codebase's "surface the fact" convention (chosen by Ethan 2026-07-23)
- DECISION: Block 4d has no CLI — mirrors research/risk/model.py exactly (the module it's explicitly modeled on has no CLI either, just the function + dataclass); no current caller needs one (chosen by Ethan 2026-07-23, overriding the earlier STATE.md Next-line note that assumed a CLI)

## Facts
- Python: system 3.10.2 at /Library/Frameworks/Python.framework/Versions/3.10/bin/python3; repo venv at .venv/ (polars 1.42.1, pytest 9.1.1, yfinance 1.5.1, pandas 2.2.3); no uv installed
- Test command: .venv/bin/python -m pytest (20 passing)
- Framework Python has no CA bundle: urllib needs ssl context with certifi.where() (fixed in yfinance_daily.fetch_listed_tickers)
- Approved plan file: /Users/ethan/.claude/plans/i-want-to-build-gleaming-ladybug.md
- Canonical architecture doc: docs/DESIGN.md (this repo)
- Gitignore bug fixed 2026-07-13 (commit a5aca9a): unanchored `data/` was hiding research/data/ from git — Ethan's commit 1ce8420 omitted store.py + crsp_daily.py despite naming them; now tracked, `/lake/` + `/data/` ignored
- Memory dir: ~/.claude/projects/-Users-ethan-Documents-Duke-Year-2-Projects-multifactor-trading-system/memory/
- C++ toolchain (2026-07-16): system AppleClang/CommandLineTools libc++ headers are broken on this machine (empty `/Library/Developer/CommandLineTools/usr/include/c++/v1`) — use Homebrew LLVM instead: `cmake -S engine -B engine/build -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++`. Build: `cmake --build engine/build -j`. Test: `ctest --test-dir engine/build --output-on-failure` (6 passing). cmake 4.4.0, Homebrew llvm 22.1.8, both brew-installed.
- AWS CLI configured (2026-07-16): account 632752099642 (root — not IAM, flagged as a later hardening item), region us-east-2. S3 bucket `ethan-personal-offload-2026` created (us-east-2, public access blocked) for personal-file disk offload — unrelated to the project's own future S3 data lake (DESIGN.md infra phase).
- numpy on this machine is backed by Apple's Accelerate BLAS (`np.show_config()`) — documented source of spurious `RuntimeWarning: divide by zero/overflow/invalid value in matmul` on fully finite, well-conditioned inputs (confirmed 2026-07-19, research/risk/model.py). Don't chase this warning as a bug without first checking actual values via `np.isnan`/`np.isinf`.
- research/risk/ deps: numpy>=2.0 (2.2.6 installed), scikit-learn>=1.5 (1.7.2 installed, pulled scipy 1.15.3 + joblib + threadpoolctl) — both now declared in pyproject.toml (numpy was previously only transitive via polars/pandas).
- research/portfolio/ deps: cvxpy>=1.5 (1.7.5 installed 2026-07-21, pulled clarabel/osqp/scs solvers + jinja2) — declared in pyproject.toml.

## Done
(architecture plan + DESIGN.md drafting 2026-07-08, Phase 1 parts 1-2 (PITStore + CRSP loader, 2026-07-09/13), gitignore fix — fully captured in docs/DESIGN.md, handoff.md status table, and STATE.md Facts (gitignore) above; dropped here for length)
- Part 2b: shared audit.py + yfinance loader (2026-07-13 session 2) — RESULT: AuditReport/audit_daily_bars extracted (crsp_daily now thin wrapper); YFinanceDailyLoader (chunked download, grid-artifact scrub, ret from Adj Close, fetch-failure flagging, shares snapshot dataset, NASDAQ symbol-file universe fetch, CLI); yfinance 1.5 pinned; `.venv/bin/python -m pytest` -> "20 passed"; crsp test YEAR-2026 time bomb defused (dt.date.today().year); SSL cert bug in symbol-file fetch fixed (certifi context)
- Rate-limit pacing built (2026-07-14) — RESULT: YFinanceClient paced (pause_s per chunk) + exponential backoff (60s doubling, max_retries then chunk→fetch_failures); throttle detected via yfinance.shared._ERRORS (batch download swallows YFRateLimitError — discovered via probe, design adjusted from raise-based retry); CLI flags --pause/--max-retries/--backoff; `.venv/bin/python -m pytest` -> "24 passed"

## Open items
- lake/yfinance_daily holds 14 year-files (k=2026-07-14T04-17-29, 2011–2024, 1.32M rows) from throttled run — ~5-10% ticker coverage, throttle-selected = unknown bias, UNUSABLE for research; wipe before phase-2 backfill (needs Ethan approval, files listed in session 2026-07-14)
- Full backfill attempt 2026-07-14 killed at 00:47 mid-2025; stdout progress lines lost to block buffering — run future backfills with `python -u`
- WRDS approved BUT Duke student access restricted to academic year — no access summer 2026; CRSP pulls resume fall semester (discovered 2026-07-13). CRSP stays the primary bars source; loader ready.
- HTCondor role ambiguous: Ethan says "run KDB in parallel with HTCondor for live trading model"; HTCondor is batch/HPC scheduler — likely better fit for research grid (backtest sweeps). Confirm intent at block-5 deep dive.
- Alt-data source selection deferred to alpha research phase
- Alpha combination weights (blend of alphas) deferred to alpha research engine
- `BrokerSimulator::inject_fill` does not validate the fill price against the order's `limit_price` (found 2026-07-16, discussing what limit orders are) — a test can currently `inject_fill(id, qty, any_price)` regardless of the order's limit, which a real broker/exchange would never do. Add a check in `inject_fill` (`engine/src/broker/BrokerSimulator.cpp`) before this skeleton is used to test any limit-order-specific behavior.

- Volume dtype bug fixed (2026-07-15) — RESULT: real cause was narrower than first suspected — only `lake/yfinance_daily/k=2026-07-14T19-06-00.570953_part=2026.parquet` (the 2026 gap-fill part) had `volume` as Int64; all other on-disk parts were already Float64. Fixed loader (`research/data/loaders/yfinance_daily.py` `_transform`, +4 lines: `pl.col("volume").cast(pl.Float64)`) so future batches can't diverge; cast the one bad file in place (lossless, no network re-fetch). Added regression test `test_transform_pins_volume_dtype_regardless_of_source` (tests/test_yfinance_loader.py). `.venv/bin/python -m pytest -q` -> "46 passed in 1.39s"; full-column `store.asof("yfinance_daily", ...).collect()` -> succeeds, `(5912600, 9)`, volume dtype Float64 (previously `SchemaError`)
- Broker simulator skeleton built (2026-07-16) — RESULT: first C++ in the repo, `engine/` (CMake + Catch2 v3.9.1, C++20): `IBrokerGateway`/`Order`/`OrderEvent` (include/broker/), `BrokerSimulator` implementing submit/cancel/poll_events + inject_ack/inject_fill/inject_reject/inject_cancel_ack test-scripting API (src/broker/), `EventJournal` CSV append-log stub (src/journal/), 6 Catch2 scenario tests (src/tests/test_broker_simulator.cpp). Blocked mid-build by two environment issues (see resolution below), both fixed: `cmake --build engine/build -j` -> clean; `ctest --test-dir engine/build --output-on-failure` -> "100% tests passed out of 6". Order gateway, position keeper, risk checks, market data handler, live AlpacaGateway are separate, unscoped.
- Dev-environment fixes enabling the above (2026-07-16) — RESULT: (1) system AppleClang/CommandLineTools libc++ headers found broken (empty dir) -> installed cmake 4.4.0 + Homebrew llvm 22.1.8 via brew, engine/ now builds with `-DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++` (see Facts); (2) disk was at 100% capacity (409Gi/460Gi used, 859Mi avail), which was the actual cause of the LLVM install itself failing (`tar: Failed to create dir`, ENOSPC) — Ethan freed real disk space (avail went 859Mi -> 23Gi), then the LLVM install succeeded cleanly; (3) set up AWS CLI + S3 bucket `ethan-personal-offload-2026` for future disk offload (see Facts) — the weather-bot project's `data/gefs`+`nbm`+`era5` (11.5GB) identified as the largest reclaimable target but not yet synced/deleted, Ethan's call pending.
- Phase 2: signals + IC measurement built + real-run verified (2026-07-16/17) — RESULT: research/signals/{momentum,reversal,low_vol}.py + registry (__init__.py), research/alpha/ic.py (compute_forward_returns, compute_ic, build_ic_series, ic_summary, CLI). 15 new tests, `.venv/bin/python -m pytest -q` -> "61 passed in 1.49s". Real run `python -m research.alpha.ic --start 2011 --end 2026` over the full lake -> momentum n=169 mean_ic=0.019 t=1.45, reversal n=180 mean_ic=0.0007 t=0.07, low_vol n=174 mean_ic=-0.0017 t=-0.10, 824.2s wall time — recorded in METRICS.md. Mid-build perf bug found+fixed: compute_forward_returns was scanning full 15y history/call (3.3s, confirmed via isolated timing) instead of a bounded forward window (1.2s after fix, matches existing low_vol/build_snapshot cost baseline); also fixed 3x redundant forward-return recomputation across signals in build_ic_series (registry-wide single pass now). SYSTEM_MAP.md + handoff.md + METRICS.md updated same session.
- Sector/industry loader built + real production run (2026-07-17) — RESULT: `YFinanceClient.sector_info()` + `YFinanceDailyLoader.load_sector_current()` (research/data/loaders/yfinance_daily.py, +~50 lines) mirroring the existing shares-snapshot pattern; dataset `yfinance_sector_current`. 1 new test, `.venv/bin/python -m pytest -q` -> "62 passed in 1.31s". Live-smoked 5 real tickers (scratch lake, 6.7s, all correct) before the real 1,500-ticker production pull: 1,497/1,500 resolved (99.8%), 1,779.9s (~29.7 min), 11 distinct sectors — recorded in docs/METRICS.md. Enables the Block 3 risk model's sector factor.
- Block 3 risk model built + real-lake verified (2026-07-19) — RESULT: research/risk/{exposures,regression,factor_covariance,specific_variance,model}.py, `RiskModel` dataclass with `.sigma()`. 17 new tests incl. a zero-noise exact-coefficient-recovery test for the regression math. `.venv/bin/python -m pytest -q` -> "79 passed in 2.65s". Real run over the real lake (985 securities, 13 factors, 179.5s) — see METRICS.md for the full 2-real-bug-plus-1-red-herring debugging chain (null sector crash, null-ret NaN corruption, benign Accelerate BLAS warnings).
- Block 4a: optimizer inputs assembly built (2026-07-20) — RESULT: research/portfolio/inputs.py, `build_optimizer_inputs`/`OptimizerInputs` (dataclass: security_ids, factor_names, B/F/D subset+reordered from RiskModel to the alpha-surviving name set kept in factor form, alpha, w_prev — all N-aligned). Alpha = placeholder equal-weight blend of all 3 signals (z-scored then shrunk 0.5x + capped ±3, satisfying DESIGN.md's "error maximizer" defense) — real alpha-combination weights still deferred to the alpha research phase. 3 new tests (test_portfolio_inputs.py). `.venv/bin/python -m pytest -q` -> "82 passed in 2.98s". Mid-build correctness fix: first draft embedded the full RiskModel (N names) alongside an alpha vector already subset to fewer names — silent row-misalignment for block 4b/4c; fixed by subsetting B/D to the alpha-surviving name set before returning. Also caught+fixed a path-naming mismatch: built under research/optimizer/ before noticing DESIGN.md's locked repo-layout (line 209) names this directory research/portfolio/ — renamed (research/optimizer/ -> research/portfolio/, tests/test_optimizer_inputs.py -> tests/test_portfolio_inputs.py) before any commit. SYSTEM_MAP.md + graphify updated same session.
- Block 4b prep: per-stock market beta built (2026-07-21) — RESULT: research/portfolio/beta.py, `compute_market_beta(bars, rebuild_date, security_ids, window_days=252)` -> rolling cov(stock, mkt)/var(mkt) beta per security; market proxy = equal-weighted mean return across the SAME `security_ids` passed in (no mcap data yet for cap-weighting). Wired into `build_optimizer_inputs`: beta computed over the alpha-surviving set, joined in (drop if missing, same convention), new `beta` field on `OptimizerInputs`. 3 new tests (test_beta.py: closed-form match against independent numpy recomputation, short-history empty guard, outsider-security-excluded-from-proxy check) + 1 shape assertion added to test_portfolio_inputs.py. Found+fixed a real test bug mid-build (not production code): test_beta.py called `store.append` 3 times under the same knowledge_ts with no `part=` — each call overwrote the previous parquet file (store.py's documented idempotency-by-filename), so only the last-appended security survived; fixed by concatenating all securities into one frame before one `append` call, matching test_risk_model.py/test_signals.py's existing convention. `.venv/bin/python -m pytest -q` -> "85 passed in 3.14s".
- Block 4b prep: ADV wired into optimizer inputs (2026-07-21) — RESULT: `build_optimizer_inputs` now also joins `lake/universe_monthly`'s `median_dollar_volume` (asof at rebuild_date) into the aligned cross-section, new `adv` field on `OptimizerInputs`, drop-if-missing same convention. Test fixture (test_portfolio_inputs.py's `populated_store`) updated to carry realistic per-ticker ADV values (was missing the column entirely before, since `universe_monthly` isn't consumed by the risk model and the earlier fixture only mirrored what risk_model.py needed). `.venv/bin/python -m pytest -q` -> "85 passed in 3.52s".
- Block 4b: constraints built (2026-07-21) — RESULT: research/portfolio/constraints.py, `build_constraints(inputs, w, gross_cap=2.0, turnover_cap=0.5, factor_exposure_cap=0.5, adv_days=5.0, book_notional=10_000_000.0)` -> list of cvxpy constraints (dollar-neutral, beta-neutral via the new per-stock beta, sector-neutral, ADV-relative position caps, turnover cap, gross leverage cap, style-factor exposure bounds). Net-leverage limit and the constant `"market"` B column deliberately left unconstrained — both collapse into dollar-neutral given Σw=0 (documented in the module docstring, not a silent gap). Borrow-availability filter deliberately NOT built — no free borrow data exists anywhere in the lake (documented gap, matches DESIGN.md's own admission this is hard to get free). New dependency: cvxpy>=1.5 (pyproject.toml + `.venv`, installed 1.7.5, bundles OSQP/Clarabel/SCS solvers). 2 new tests (test_constraints.py) — not just shape checks: builds a real cvxpy Problem with a non-trivial (alpha-driven, ridge-regularized) objective so the solved `w` actually explores constraint boundaries, then asserts every constraint holds on the solution, plus one test with an artificially tight ADV cap confirming it actually binds (not just holds slack). `.venv/bin/python -m pytest -q` -> "87 passed in 3.26s".
- Block 4c: QP solve built (2026-07-22) — RESULT: research/portfolio/solve.py, `solve_qp(inputs, risk_aversion=5.0, cost_penalty=10.0, **constraint_kwargs)` -> `(w, problem)`. Objective `max αᵀw − λ·wᵀΣw − κ·‖w−w_prev‖²`, risk penalty computed in factor form (`quad_form(Bᵀw, F, assume_PSD=True) + Σ(D_i·w_i²)`, never materializes the full N×N Σ) — `assume_PSD=True` because F (Ledoit-Wolf shrunk) is theoretically PSD by construction, skipping cvxpy's own check avoids a spurious DCP failure from float-rounding negative eigenvalues. The κ soft-turnover term is deliberately layered ON TOP of block 4b's hard `turnover_cap` constraint, not a replacement. 3 new tests (test_solve.py): full constraint-satisfaction check on a real solve (mirrors test_constraints.py's rigor), zero-alpha-flat-start returns exactly w=0 (closed-form-ish, deterministic), higher λ provably shrinks gross exposure (monotonicity property, not just "doesn't error"). `.venv/bin/python -m pytest -q` -> "90 passed in 4.38s".
- Block 4d: end-to-end orchestrator built (2026-07-23) — RESULT: research/portfolio/model.py, `TargetPortfolio` (dataclass: rebuild_date, security_ids, weights, objective_value, status) + `build_target_portfolio(store, rebuild_date, w_prev=None, lookback_years=3, shrink=0.5, cap=3.0, risk_aversion=5.0, cost_penalty=10.0, knowledge_ts=None, **constraint_kwargs)` chaining `build_optimizer_inputs` → `solve_qp` into one call, mirroring `research/risk/model.py`'s `build_risk_model` role (confirmed by reading that file: it has no CLI, so block 4d skips one too, correcting the earlier STATE.md Next-line assumption). `None` propagates from `build_optimizer_inputs`; a non-`None` result still surfaces a non-optimal `status` rather than raising (see Decisions). 3 new tests (test_model.py, reusing test_portfolio_inputs.py's `populated_store` fixture pattern): end-to-end shape/status/dollar-neutral check, None on insufficient history, `**constraint_kwargs` forwarding verified via a tight `gross_cap` that actually binds. `.venv/bin/python -m pytest -q` -> "93 passed in 4.20s". **Block 4 (Optimizer) now fully built** — SYSTEM_MAP.md's "Not yet started" line no longer lists any part of research/portfolio/.

## Failed attempts
(resolved this session, kept for the reasoning chain — see Done above for full context)
- ATTEMPT 1 [L1]: build_risk_model against real lake -> `TypeError: '<' not supported between NoneType and str` in exposures.py's `sorted(unique_sectors)` -> CAUSE: 3 real tickers have `sector=None` -> FIXED: filter `is_not_null()` before reference-sector selection.
- ATTEMPT 2 [L1]: retry -> completed (exit 0) but emitted `RuntimeWarning: divide by zero/overflow/invalid value in matmul` across regression.py/factor_covariance.py/model.py -> hypothesis: rank-deficient exposure matrix -> DISPROVEN (checked matrix_rank across all 43 real dates, zero deficient).
- ATTEMPT 3 [L2, new hypothesis per D6]: near-singular (high condition number) matrix -> DISPROVEN (max condition number ~20, tiny; no huge coefficients found either, though that check itself had a NaN-comparison flaw worth noting: `max()` over values containing NaN doesn't reliably surface it).
- ATTEMPT 4 [L2]: re-read the actual data path -> found the loader's documented first-bar-of-year null `ret` was NOT filtered before `.to_numpy()` in regression.py, confirmed 1-4 null-ret securities on 12 of 43 real dates -> FIXED: filter `ret.is_not_null()` at regression.py's own boundary. Re-ran with `-W error::RuntimeWarning` -> STILL fired on 2023-01-31, but isolated inputs (X, y) were completely clean (no null/NaN/Inf, condition number fine) -> root-caused to Apple Accelerate BLAS (`np.show_config()`), confirmed benign via explicit `np.isnan`/`np.isinf` on every output value (B, F, D, Σ) — documented in code, not a bug, closed.
